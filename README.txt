To run this program, run the NaiveBayesPrintout class, which contains a main method. 
All of the experimental results will be printed to the console.

There is a text file titled "Lab Report" which contains answers to questions listed in the assignment, as well as a copy 
of the experimental results that print out when running the program.